const createNewtodo = (
  title,
  description,
  category,
  completed,
  dueDate,
  priority
) => ({
  title,
  description,
  category,
  completed,
  dueDate,
  priority,
});

export default createNewtodo;
